spring:
  application:
    name: "admin-service"

  thymeleaf:
    cache: false
    enabled: true
    mode: HTML
  devtools:
    restart:
      enabled: true
  data:
    rest:
      base-path: ""

server:
  port: 8081

eureka:
  client:
    service-url:
      default-zone: "http://localhost:8761/eureka/"

